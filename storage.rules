rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // 모든 인증된 사용자에게 이미지 읽기/쓰기 허용 (프로덕션 전 개발용)
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
